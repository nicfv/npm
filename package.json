{
  "scripts": {
    "start": "npm i && tsc -v && npm start --workspaces && typedoc --version && typedoc",
    "stop": ".github/workflows/clean"
  },
  "workspaces": [
    "packages/t6",
    "packages/smath",
    "packages/*"
  ],
  "devDependencies": {
    "typedoc": "0.27.6",
    "typescript": "5.7.2"
  }
}