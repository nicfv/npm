import { X } from 'exray';

// 5 is greater than 4
X.gt(5, 4);
X.true(5 > 4);

// 2 is less than or equal to 2
X.le(2, 2);
X.true(2 <= 2);

// 3 is not equal to 0
X.ne(3, 0)
X.true(3 !== 0);
X.false(3 === 0);