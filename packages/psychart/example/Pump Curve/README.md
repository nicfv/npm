# Pump Chart

This example shows how to set up a pump chart with minimal configuration. It uses the `plot()` method to plot a single data point. When hovering over it, a tooltip will be displayed with more information about the data.